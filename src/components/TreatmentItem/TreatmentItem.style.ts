import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginRight: 53,
    alignItems: 'center',
  },
  image: {
    width: 82,
    height: 82,
    borderRadius: 12,
    marginBottom: 8,
  },

  text: {
    fontWeight: '500',
    fontSize: 16,
    lineHeight: 26,
  },
});
